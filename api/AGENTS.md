## API Versioning

All newly created API endpoints MUST be implemented under `api/v2` and registered under the `/v2` route.

A new endpoint MAY use another API version only when an explicit requirement requires that version.
